package classes;

    @SuppressWarnings({"InitializerMayBeStatic", "UnnecessaryReturnStatement"})
    class User extends users{
    
    public User(int user_id,String user_name,String user_password,String user_email,int date_of_birth,String place_of_birth){
        super(user_id, user_name, user_password, user_email, date_of_birth, place_of_birth);
    
    }
    
    
    }

